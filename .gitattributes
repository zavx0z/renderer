# Pinned external snapshots are byte-exact inputs whose upstream whitespace is
# part of the locked digest. Do not normalize or flag their source formatting.
specifications/sources/**/*.html -whitespace
